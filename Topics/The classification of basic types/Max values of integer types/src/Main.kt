fun main() {
    val listOfValues = mutableListOf(
        Byte.MAX_VALUE,
        Short.MAX_VALUE,
        Int.MAX_VALUE,
        Long.MAX_VALUE,
    )
    for (value in listOfValues) println(value)
}